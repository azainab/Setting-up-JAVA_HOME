###mac



'touch ~/.bash_profile; '

'open ~/.bash_profile'

'export JAVA_HOME=$(/usr/libexec/java_home -v 1.8)'

save and close the terminal

open a new terminal and type 'java -version'



#### windows

echo %JAVA_HOME%

JAVA_HOME as C:\Program Files\Java\jdk-12.0.2
path variable as %JAVA_HOME%\bin

make sure there is not path assigned as C:\Program Files (x86)\Common Files\Oracle\Java\javapath

java -version and java_home might be different versions make sure they are the same version


## java heap size
java -XX:+PrintFlagsFinal -version | grep -iE 'heapsize|permsize|threadstacksize'
